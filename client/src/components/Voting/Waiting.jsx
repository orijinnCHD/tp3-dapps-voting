import React from 'react';

const Waiting = () => {
    return (
        <div className="waiting-container">
            <p>Attendez que l'administrateur change de session</p>
        </div>
    );
};

export default Waiting;